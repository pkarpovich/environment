settings:
  log_dir: "./logs"

scripts:
  calendar:
    command: "./calendar.sh"
    restart_policy: "always"
    max_restarts: 5
    cron: "0 0 * * * * *"

  twitch_nfo_generator:
    command: "./twitch_nfo_generator.sh"
    restart_policy: "always"
    max_restarts: 5
    cron: "0 0 * * * * *"
